---

# task: install.yaml

# Create mount point

# vars:
#   isDirCreated: false

- name: create directory on host
  file: path=/var/data state=directory mode=0755
#   - stat: path/var/data
#     register: st
